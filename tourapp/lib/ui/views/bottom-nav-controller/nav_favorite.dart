import 'package:flutter/material.dart';

class Nav_Favorite extends StatelessWidget {
  const Nav_Favorite({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Home'),
    );
  }
}
